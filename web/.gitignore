node_modules/
.next/
out/
dist/
coverage/
*.tsbuildinfo
next-env.d.ts

# Secrets. This repo is public.
.env
.env.local
.env*.local
*serviceAccount*.json
*-adminsdk-*.json

# `vercel link` writes a project id here. It also appends `.env*`, which shadows the
# tracked `.env.local.example` - scripts/check-gitignore-scope.sh catches that, and the
# three .env lines above already cover everything `vercel env pull` writes.
.vercel
